# 开发变更日志

所有代码和部署配置修改都先在本地 VSCode 完成，再推送云仓库，最后从云仓库部署服务器。

## 2026-09-21

- 建立正式/测试环境隔离约定：正式环境使用 80/443 和正式数据卷，测试环境使用 18000 和 `jbb-staging-data` 数据卷。
- 新增 `docker-compose.staging.yml`，测试环境单独构建应用容器，不启动正式 Caddy，不触碰正式容器。
- 在正式 Compose 和示例配置中加入 `JBB_ENVIRONMENT` 标识。
- 记录服务器端已完成的隔离验证：正式容器保持 healthy，测试容器 `jubianbian-staging-app-1` 在本机健康检查返回 200。
- 方舟 Key 按当前约定由两个环境共用；测试调用会消耗同一账户额度。
- 初始化本地 Git 前补充忽略 `node_modules/`，避免把本地依赖目录提交到云仓库。
- 增加测试环境自动部署脚本和 systemd 定时器：服务器每 2 分钟跟随 GitHub `main`，只更新测试环境并执行健康检查。
- 增加请求/提供商/任务阶段日志、轮转日志文件、任务事件时间线、`/api/metrics` 运行指标和每 5 分钟测试环境监控定时器。
- 修复测试自动部署的启动竞态：健康检查改为最多 30 次重试；Git 操作和健康检查绕过服务器无效代理；同步代码后恢复脚本执行权限并自动刷新 systemd 单元；systemd 入口统一通过 Bash 调用，避免 Git 文件模式导致 `203/EXEC`。
- 优化识别链路：移除处理任务中的人为等待，方舟文件就绪轮询默认缩短为 1 秒（仍可通过环境变量调整），并补充场景数、对白数、动作数及动作主语告警等剧本质量指标；保留 `ARK_VIDEO_FPS=0.5`，待真实阶段日志样本后再决定是否调整采样率。
- 完善剧本归一化：场景自动补齐对白人物，动作主语修复会参考人物档案，缺少人物档案时生成可追踪的占位档案，减少人物前后不一致。
- 修复无时间戳 block 被类型排序打乱的问题：没有时间戳时保持模型原始事件顺序，并让对白人物可作为后续连续动作的主语上下文。
- 加强台词不可改写约束与本地完整性告警：只做标点/空白归一化，检测混合说话人而不静默改写；时间戳不完整时保持模型原始顺序，避免对白串句。
- 为 VO 保留 `voKind`（`os`/`narration`/`memory`/`phone`/`unknown`），前端和导出明确区分内心独白；人物档案补充 `firstAppearance`，并统计首登介绍缺失告警。
